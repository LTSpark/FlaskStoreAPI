get:
  tags:
    - User
  description: Returns all database users
  responses:
    "200":
      description: List of users
      content:
        application/json:
          schema:
            $ref: schema/user_definition.yaml